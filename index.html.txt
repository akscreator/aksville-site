<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AKSVILLE | Digital Architect</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&family=JetBrains+Mono&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #00F2FF;
            --live-red: #FF003C;
            --bg-body: #0a0a0a;
            --bg-card: #121212;
            --glass: rgba(255, 255, 255, 0.03);
            --border: rgba(0, 242, 255, 0.2);
            --text-main: #ffffff;
            --text-dim: #999;
            --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--bg-body); }
        ::-webkit-scrollbar-thumb { background: var(--primary); border-radius: 10px; }

        * { margin: 0; padding: 0; box-sizing: border-box; cursor: none; } /* Cursor none for custom pointer */

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        /* --- Custom Cursor --- */
        #cursor {
            width: 20px; height: 20px; border: 2px solid var(--primary);
            border-radius: 50%; position: fixed; pointer-events: none;
            z-index: 9999; transition: transform 0.1s; transform: translate(-50%, -50%);
        }

        /* --- Progress Bar --- */
        #scrollProgress {
            position: fixed; top: 0; left: 0; height: 4px;
            background: var(--primary); z-index: 1001; width: 0%;
        }

        /* --- Navbar --- */
        nav {
            position: fixed; top: 0; width: 100%; height: 75px;
            background: rgba(10, 10, 10, 0.8); backdrop-filter: blur(15px);
            border-bottom: 1px solid var(--border);
            display: flex; justify-content: space-between; align-items: center;
            padding: 0 6%; z-index: 1000;
        }

        .logo { font-size: 1.6rem; font-weight: 900; letter-spacing: 3px; color: var(--primary); }

        .nav-links { display: flex; list-style: none; gap: 40px; }
        .nav-links a { 
            text-decoration: none; color: var(--text-main); font-weight: 700; 
            font-size: 0.85rem; text-transform: uppercase; transition: var(--transition);
        }
        .nav-links a:hover { color: var(--primary); text-shadow: 0 0 10px var(--primary); }

        /* --- Hero --- */
        .hero {
            height: 90vh; display: flex; flex-direction: column;
            justify-content: center; align-items: center; text-align: center;
            padding: 0 10%; background: linear-gradient(to bottom, rgba(0,242,255,0.05) 0%, transparent 100%);
        }

        .live-tag {
            display: flex; align-items: center; gap: 10px; padding: 8px 20px;
            border: 1px solid var(--border); border-radius: 50px; margin-bottom: 25px;
            background: var(--glass); font-family: 'JetBrains Mono', monospace; font-size: 0.8rem;
        }
        .pulse-dot { width: 10px; height: 10px; border-radius: 50%; background: var(--live-red); animation: pulse 1.5s infinite; }

        @keyframes pulse { 0% { box-shadow: 0 0 0 0 rgba(255, 0, 60, 0.7); } 70% { box-shadow: 0 0 0 15px rgba(255, 0, 60, 0); } 100% { box-shadow: 0 0 0 0 rgba(255, 0, 60, 0); } }

        .hero h1 { font-size: clamp(3rem, 10vw, 6rem); text-transform: uppercase; line-height: 0.9; margin-bottom: 20px; }
        .hero h1 span { color: transparent; -webkit-text-stroke: 1.5px var(--primary); filter: drop-shadow(0 0 10px var(--primary)); }
        
        .cta-group { display: flex; gap: 20px; margin-top: 40px; }
        .btn {
            padding: 16px 40px; border: 1px solid var(--primary); border-radius: 0px; /* Sharp tech look */
            text-transform: uppercase; font-weight: 900; transition: var(--transition); text-decoration: none;
        }
        .btn-fill { background: var(--primary); color: #000; }
        .btn-fill:hover { background: transparent; color: var(--primary); box-shadow: 0 0 25px var(--primary); }

        /* --- Video Section --- */
        section { padding: 120px 8%; }
        .title { font-size: 2.5rem; margin-bottom: 60px; border-left: 5px solid var(--primary); padding-left: 20px; text-transform: uppercase; }

        .video-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 30px; }
        .vid-card { 
            background: var(--bg-card); border: 1px solid var(--border); 
            transition: var(--transition); position: relative; overflow: hidden;
        }
        .vid-card:hover { border-color: var(--primary); transform: translateY(-10px); }
        .vid-thumb { width: 100%; aspect-ratio: 16/9; background-size: cover; background-position: center; }
        .vid-info { padding: 25px; }

        /* --- Milestone Timeline --- */
        .timeline { position: relative; max-width: 1000px; margin: 0 auto; }
        .timeline::before { content: ''; position: absolute; left: 50%; width: 1px; height: 100%; background: var(--border); }
        .m-item { margin: 40px 0; width: 50%; padding: 0 40px; position: relative; }
        .m-item:nth-child(even) { margin-left: auto; }
        .m-content { background: var(--bg-card); padding: 30px; border: 1px solid var(--border); }
        .m-content h3 { color: var(--primary); margin-bottom: 10px; }
        .m-dot { position: absolute; top: 15px; right: -8px; width: 16px; height: 16px; background: var(--primary); border-radius: 50%; box-shadow: 0 0 10px var(--primary); }
        .m-item:nth-child(even) .m-dot { left: -8px; }

        /* --- Contact --- */
        .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 100px; }
        .contact-input { 
            width: 100%; padding: 15px; margin-bottom: 20px; 
            background: #111; border: 1px solid var(--border); color: #fff;
            font-family: 'JetBrains Mono'; outline: none;
        }
        .contact-input:focus { border-color: var(--primary); box-shadow: 0 0 15px var(--border); }

        footer { padding: 80px 0; border-top: 1px solid var(--border); text-align: center; background: #050505; }
        .socials a { color: #fff; font-size: 1.5rem; margin: 0 20px; transition: 0.3s; }
        .socials a:hover { color: var(--primary); }

        /* --- Responsive --- */
        @media (max-width: 900px) {
            .contact-grid { grid-template-columns: 1fr; gap: 50px; }
            .timeline::before { left: 0; }
            .m-item { width: 100%; padding-left: 30px; }
            .m-dot { left: -8px !important; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

    <div id="cursor"></div>
    <div id="scrollProgress"></div>

    <nav>
        <div class="logo">AKSVILLE</div>
        <ul class="nav-links">
            <li><a href="#home">Home</a></li>
            <li><a href="#videos">Feed</a></li>
            <li><a href="#about">Milestones</a></li>
            <li><a href="#contact">Collab</a></li>
        </ul>
        <div style="color: var(--primary); font-family: 'JetBrains Mono';">v2.0.26</div>
    </nav>

    <section class="hero" id="home">
        <div class="live-tag" id="liveStatus">
            <div class="pulse-dot"></div>
            <span id="statusText">DETECTING LIVE STREAM...</span>
        </div>
        <h1>THE ART OF <span>CONTENT</span></h1>
        <p style="color: var(--text-dim); max-width: 650px; font-size: 1.1rem;">
            AKSVILLE: Decoding technology and gaming through cinematic lenses. 
            Latest tech breakdowns and high-performance gaming strategies.
        </p>
        <div class="cta-group">
            <a href="https://youtube.com/channel/UCvZDYPVGA-ATIGRwvmQ35sw" target="_blank" class="btn btn-fill">Subscribe</a>
            <a href="#videos" class="btn" style="color: #fff;">Explore Feed</a>
        </div>
    </section>

    <section id="videos">
        <h2 class="title">Transmission <span>Feed</span></h2>
        <div class="video-grid" id="ytFeed">
            </div>
    </section>

    <section id="about" style="background: #080808;">
        <h2 class="title">System <span>Log</span></h2>
        <div class="timeline">
            <div class="m-item">
                <div class="m-dot"></div>
                <div class="m-content">
                    <h3>Channel Launch</h3>
                    <p>Initialization of the AKSVILLE hub. First deep-dive video into high-performance PC architecture.</p>
                </div>
            </div>
            <div class="m-item">
                <div class="m-dot"></div>
                <div class="m-content">
                    <h3>5,000 Nodes</h3>
                    <p>Community growth surpassed 5K subscribers. Implemented Discord integration for direct fan access.</p>
                </div>
            </div>
            <div class="m-item">
                <div class="m-dot"></div>
                <div class="m-content">
                    <h3>Creator Gear Pro</h3>
                    <p>Signed professional gear partnership. Production quality upgraded to 4K 60FPS standard.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="contact">
        <div class="contact-grid">
            <div>
                <h2 class="title" style="border: none; padding: 0;">Direct <span>Uplink</span></h2>
                <p style="color: var(--text-dim);">Have a proposal? Let's build the future of content together. Response time within 24 hours.</p>
            </div>
            <form>
                <input type="text" placeholder="IDENTITY" class="contact-input" required>
                <input type="email" placeholder="EMAIL_ADDRESS" class="contact-input" required>
                <textarea rows="5" placeholder="TRANSMISSION_DATA" class="contact-input" required></textarea>
                <button type="submit" class="btn btn-fill" style="width: 100%;">Send Data</button>
            </form>
        </div>
    </section>

    <footer>
        <div class="socials">
            <a href="https://youtube.com/channel/UCvZDYPVGA-ATIGRwvmQ35sw" target="_blank"><i class="fab fa-youtube"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
        </div>
        <p style="color: var(--text-dim); margin-top: 30px; font-size: 0.8rem; letter-spacing: 2px;">© 2026 AKSVILLE CORE S78</p>
    </footer>

    <script>
        // Custom Cursor Movement
        const cursor = document.getElementById('cursor');
        document.addEventListener('mousemove', e => {
            cursor.style.left = e.clientX + 'px';
            cursor.style.top = e.clientY + 'px';
        });

        // Scroll Progress Bar
        window.onscroll = function() {
            let winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            let height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            let scrolled = (winScroll / height) * 100;
            document.getElementById("scrollProgress").style.width = scrolled + "%";
        };

        // Live Status Simulation
        async function getLiveStatus() {
            const statusText = document.getElementById('statusText');
            // Mocking logic: High chance to be "Live" for portfolio display
            const isLive = Math.random() > 0.4;
            statusText.innerText = isLive ? "SIGNAL: LIVE BROADCASTING" : "SIGNAL: OFFLINE";
            if(!isLive) document.querySelector('.pulse-dot').style.background = "#555";
        }

        // RSS Video Feed
        async function fetchYT() {
            const cid = 'UCvZDYPVGA-ATIGRwvmQ35sw';
            const api = `https://api.rss2json.com/v1/api.json?rss_url=${encodeURIComponent('https://www.youtube.com/feeds/videos.xml?channel_id='+cid)}`;
            
            try {
                const res = await fetch(api);
                const data = await res.json();
                const feed = document.getElementById('ytFeed');
                feed.innerHTML = '';

                data.items.slice(0, 3).forEach(v => {
                    const id = v.guid.split(':')[2];
                    feed.innerHTML += `
                        <div class="vid-card">
                            <a href="${v.link}" target="_blank" style="text-decoration:none; color:inherit;">
                                <div class="vid-thumb" style="background-image:url('https://img.youtube.com/vi/${id}/maxresdefault.jpg')"></div>
                                <div class="vid-info">
                                    <p style="color:var(--primary); font-size: 0.7rem; font-family: 'JetBrains Mono'; margin-bottom: 5px;">[DATA_CHUNK_LATEST]</p>
                                    <h4 style="font-weight:900;">${v.title}</h4>
                                </div>
                            </a>
                        </div>
                    `;
                });
            } catch(e) { console.error("Feed Error"); }
        }

        // Init
        getLiveStatus();
        fetchYT();
    </script>
</body>

</html>
